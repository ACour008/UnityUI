public class UISpriteManager : Singleton<UISpriteManager>
{
    public UISpriteAtlas spriteAtlas;

    // Typically this will be called from the Shell, but for now, UICanvas calls it.
    public void LoadSprites()
    {
        
    }
}